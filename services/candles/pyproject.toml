[project]
name = "candles"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "loguru",
    "pydantic-settings",
    "quixstreams",
]
